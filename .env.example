# Database
DATABASE_URL="postgresql://user:password@localhost:5432/nodebase?schema=public"

# Encryption key for secure credential storage (use a long random string)
ENCRYPTION_KEY="replace-with-a-long-random-string"

# Public base URL for the app (use your ngrok URL when testing external webhooks)
NEXT_PUBLIC_API_URL="http://localhost:3000"

# Better Auth
BETTER_AUTH_SECRET="replace-with-the-secret"
BETTER_AUTH_URL="http://localhost:3000"

# OAuth (optional)
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Polar billing (optional)
POLAR_ACCESS_TOKEN=""
POLAR_ENV="sandbox" # or "production"
POLAR_PRODUCT_ID=""
POLAR_SUCCESS_URL="http://localhost:3000"

# Sentry
SENTRY_AUTH_TOKEN=""

# Note: Do NOT put OpenAI/Anthropic/Gemini API keys here.
# Add them inside the app under Dashboard → Credentials (stored encrypted).
